# 电批力控测试参数说明

本文档对应测试脚本：

```text
lerobot_robot_screw/tests/test_torque_insert_hardware.py
```

该脚本用于联合控制 UR3 机械臂和电批完成螺丝插入、动态减速、末段纯旋转以及达到目标力矩后的协调停止。本文所有默认值均以当前脚本源码为准。

## 1. 当前控制过程

脚本固定采用 UR3 与电批联合控制，完整控制过程如下：

1. 连接 UR3 和电批服务，并检查 CAN 反馈是否正常。
2. 电批以 `--screw-speed-rpm` 启动。
3. 机械臂沿工具坐标系 Z 轴移动，最大移动距离由 `--max-insert-depth-mm` 限制。
4. 动态电批调速固定启用；力矩接近目标值时，电批从高速逐步降至 `--torque-crawl-rpm`。
5. 动态机械臂速度固定启用；机械臂根据电批实测转速和目标转速同步降低插入速度。
6. 当电批目标转速降到 `--rotation-only-speed-rpm` 或以下时，机械臂锁定当前 TCP 位置，不再平移，电批继续旋转。
7. 原始力矩首次达到 `--torque-stop-nm`，或过滤力矩连续满足确认条件后，程序协调停止机械臂和电批。

关键关系如下：

```text
开始降速力矩 = torque-stop-nm × torque-slowdown-ratio
完全爬行力矩 = torque-stop-nm × dynamic-screw-full-crawl-ratio
机械臂基础速度 = insert-speed-mm-s
机械臂实际速度 ≈ 基础速度 × 动态速度比例
后期纯旋转触发 = 电批目标转速 ≤ rotation-only-speed-rpm
后端紧急保护力矩 = torque-stop-nm × backend-torque-guard-ratio
```

以当前常用配置为例：

```text
最终停止力矩：3.5 Nm
开始降速比例：0.35  → 约 1.225 Nm 开始降低电批转速
完全爬行比例：0.65  → 约 2.275 Nm 降至爬行转速
纯旋转转速：80 rpm → 电批目标转速降到 80 rpm 后机械臂停止平移
爬行转速：30 rpm
后端保护比例：2.0 → 电批后端紧急保护值为 7.0 Nm
```

## 2. 运行模式与连接参数

测试脚本现在固定连接并控制 UR3，不再提供 `--moveL` 或 `--movel` 参数，也不再包含纯电批运行分支。

### `--robot-host`

- 默认值：`192.168.1.5`
- UR3 的 IP 地址。
- 脚本启动时始终使用该地址连接 UR3。

### `--robot-connect-timeout-s`

- 默认值：`10.0`
- 单位：秒。
- 检查 UR RTDE 和脚本端口以及初始化接口时的连接超时。

### `--screw-host`

- 默认值：`127.0.0.1`
- 电批服务地址。
- 电批服务运行在本机时保持默认值即可。

### `--screw-port`

- 默认值：`5055`
- 电批服务端口，必须与启动 `screw_server` 时的 `--port` 一致。

### `--screw-timeout-s`

- 默认值：`2.0`
- 单位：秒。
- 单次电批 Socket 请求的超时时间。
- 该参数不是 CAN 反馈超时；CAN 反馈超时由电批服务自身控制。

## 3. 机械臂插入参数

### `--insert-sign`

- 默认值：`1`
- 可选值：`1` 或 `-1`。
- 决定机械臂沿工具 Z 轴的插入方向。
- 如果机械臂向远离螺丝孔的方向运动，应检查该参数，但必须先在低速、小距离下验证。

### `--max-insert-depth-mm`

- 默认值：`1.0`
- 单位：毫米。
- 机械臂允许前进的最大距离。
- 到达该距离仍未达到目标力矩时，测试会失败并停止。
- 该参数是运动范围上限，不代表机械臂一定会走完整段距离。

### `--insert-speed-mm-s`

- 默认值：`0.5`
- 单位：毫米/秒。
- 机械臂的基础插入速度。
- 实际速度会在该值基础上乘以动态比例。

例如基础速度为 `15 mm/s`，动态比例为 `0.4` 时：

```text
实际插入速度约为 15 × 0.4 = 6 mm/s
```

速度过大可能造成机械臂轴向推进快于螺丝旋入速度，从而推动工件。

### `--linear-acc-mm-s2`

- 默认值：`20.0`
- 单位：毫米/秒²。
- 普通直线运动的加速度参数。
- 动态插入主要使用连续 `servoL` 控制，因此该参数对动态阶段的作用小于对普通 `moveL` 的作用。

### `--stop-linear-acc-mm-s2`

- 默认值：`2000.0`
- 单位：毫米/秒²。
- 最终达到力矩阈值后，机械臂执行直线停止时使用的减速度。
- 数值越大，机械臂停止越快，但冲击也可能更明显。
- 该参数只负责最终协调停止，不负责进入末段纯旋转。

### `--motion-timeout-s`

- 默认值：`10.0`
- 单位：秒。
- 整个机械臂插入和末段纯旋转控制允许持续的最长时间。
- 纯旋转阶段也包含在该时间内。
- 如果螺丝需要较长时间才能达到目标力矩，建议设置为 `20`～`30` 秒。
- 超时后程序会认为运动未正常达到力矩并执行安全停止。

## 4. 电批基础参数

### `--screw-speed-rpm`

- 默认值：`10.0`
- 单位：rpm。
- 电批初始目标转速。
- 正负号决定旋转方向，绝对值决定目标转速。
- 当前设备即使设置很高的目标值，实测转速也可能受电机或驱动器上限限制。例如设置 `600 rpm` 时，实际可能只达到约 `220 rpm`。
- 动态调速会使用启动后测得的真实最高转速作为有效高速上限，避免在硬件饱和区间发送无效减速指令。

### `--torque-stop-nm`

- 无默认可用值，必须显式传入。
- 单位：Nm。
- 本次拧紧的最终停止力矩。
- 原始力矩任意一个有效样本达到该值时，会立即触发硬阈值停止。
- 过滤力矩连续达到该值时，也会触发确认停止。
- 该值不能盲目提高，必须考虑电批、电机、批头、螺丝、螺纹和工件允许的最大力矩。

### `--backend-torque-guard-ratio`

- 默认值：`2.0`
- 必须大于 `1.0`。
- 电批后端紧急保护力矩相对正常停止力矩的倍数。

计算方式：

```text
后端紧急保护值 = torque-stop-nm × backend-torque-guard-ratio
```

例如：

```text
torque-stop-nm = 3.5
backend-torque-guard-ratio = 2.0
后端保护值 = 7.0 Nm
```

正常情况下应由上层协调控制在 `3.5 Nm` 停止，只有上层程序来不及停止或通信异常时才依靠后端保护。该比例过小可能导致电批先停止、机械臂后停止；过大则会降低后端保护作用。

### `--torque-crawl-rpm`

- 默认值：`60.0`
- 单位：rpm。
- 接近最终力矩时的最低爬行转速。
- 较低的爬行转速通常能获得更高、更稳定的最终拧紧力矩，并减少惯性过冲。
- 数值太低可能导致电机堵转、控制时间过长或无法继续克服静摩擦。

## 5. 力矩采样、过滤与停止判定

### `--torque-poll-s`

- 默认值：`0.005`
- 单位：秒。
- 力矩状态轮询周期，默认约为 `200 Hz`。
- 更小的值可以更快发现力矩上升，但会增加通信和 CPU 负担。
- 实际响应速度还受 CAN 反馈频率、电批服务处理速度和操作系统调度影响。

### `--torque-filter-window`

- 默认值：`5`
- 单位：样本数。
- 力矩中值滤波窗口。
- 窗口越大，抗噪声能力越强，但过滤力矩反应越慢。
- 原始力矩硬阈值不等待此窗口，因此该参数不会取消瞬时硬停止保护。

### `--torque-confirm-samples`

- 默认值：`3`
- 单位：连续样本数。
- 过滤力矩达到最终阈值后，需要连续满足多少个样本才确认停止。
- 数值越大，抗抖动能力越强，但过滤路径的停止会略晚。
- 原始力矩达到硬阈值时不会等待这些确认样本。

### `--torque-min-active-s`

- 默认值：`0.2`
- 单位：秒。
- 电批启动后的力矩判定屏蔽时间。
- 用于忽略启动瞬间的冲击或异常反馈。
- 设置过大会导致真正的早期堵转不能及时被上层阈值检测。

## 6. 动态电批减速参数

动态电批减速现在是固定功能，不再提供 `--dynamic-screw-speed` 或 `--no-dynamic-screw-speed` 开关。

### `--torque-slowdown-ratio`

- 默认值：`0.65`
- 范围：`0` 到 `1`，不包括端点。
- 决定从最终力矩的多少比例开始降低电批速度。

计算方式：

```text
开始减速力矩 = torque-stop-nm × torque-slowdown-ratio
```

数值越小，减速越早；数值越大，减速越晚。

### `--dynamic-screw-full-crawl-ratio`

- 默认值：`0.70`
- 必须满足：

```text
torque-slowdown-ratio < dynamic-screw-full-crawl-ratio < 1
```

- 力矩达到该比例附近时，电批目标速度会降到 `--torque-crawl-rpm`。

计算方式：

```text
完全爬行力矩 = torque-stop-nm × dynamic-screw-full-crawl-ratio
```

减速区间是：

```text
[开始减速力矩, 完全爬行力矩]
```

这个区间越宽，减速越平缓；区间越窄，转速下降越集中。

### 两个力矩比例是否重复？

不重复。它们分别定义动态减速曲线的两个端点：

- `--torque-slowdown-ratio`：减速起点。在此之前保持有效高速。
- `--dynamic-screw-full-crawl-ratio`：减速终点。到达此处时已经降至 `--torque-crawl-rpm`。

在两个端点之间，电批目标转速从有效高速连续插值到爬行转速。例如最终力矩为 `3.5 Nm`、两个比例分别为 `0.35` 和 `0.65` 时：

```text
0 ～ 1.225 Nm：保持有效高速
1.225 ～ 2.275 Nm：连续动态减速
2.275 ～ 3.5 Nm：以爬行转速继续收紧
3.5 Nm：协调停止
```

### `--dynamic-screw-speed-prediction-s`

- 默认值：`0.10`
- 单位：秒。
- 根据近期力矩上升斜率，预测未来多少秒后的力矩。
- 用于在力矩快速上升时提前减速，减少过冲。
- 数值越大，减速越提前，但对噪声越敏感。

### `--dynamic-screw-prediction-min-rise-nm`

- 默认值：`0.15`
- 单位：Nm。
- 近期力矩至少净上升这么多，才允许启用斜率预测。
- 用于避免把普通力矩噪声误判为快速上升。
- 数值过小容易误触发提前减速；数值过大则可能无法及时预测真实力矩突升。

### `--dynamic-screw-speed-command-interval-s`

- 默认值：`0.05`
- 单位：秒。
- 两次电批调速命令之间的最短间隔。
- 默认最多约每秒发送 20 次速度调整。
- 间隔过小会增加 Socket/CAN 命令压力；间隔过大会使减速呈明显阶梯状。

### `--dynamic-screw-speed-slew-rpm-s`

- 默认值：`3000.0`
- 单位：rpm/s。
- 电批目标转速允许下降的最大变化速率。
- 数值越大，允许更快降速；数值越小，减速过程更平滑但可能来不及应对快速力矩上升。
- 当控制器判定已进入完全爬行区间时，可以直接降至爬行转速。

### `--dynamic-screw-speed-min-command-delta-rpm`

- 默认值：`20.0`
- 单位：rpm。
- 新旧目标转速至少相差这么多才真正发送调速命令。
- 用于减少过于频繁的小幅调速命令。
- 数值过大时，速度变化会呈大台阶；数值过小时，会增加命令频率。

## 7. 动态机械臂进给参数

动态机械臂进给现在是固定功能，不再提供 `--dynamic-insert-speed` 或 `--no-dynamic-insert-speed` 开关。控制器始终使用连续 `servoL`，机械臂插入速度始终跟随电批转速变化，末段纯旋转也始终可用。

### `--dynamic-speed-servo-dt`

- 默认值：`0.008`
- 单位：秒。
- `servoL` 控制周期，默认约 `125 Hz`。
- 数值过大时运动可能不够连续；数值过小会增加实时控制负担。
- 一般不建议在没有验证的情况下随意修改。

### `--dynamic-speed-min-valid-rpm`

- 默认值：`30.0`
- 单位：rpm。
- 实测电批速度达到该值后，才用于建立真实基准转速。
- 低于该值的启动瞬间反馈不会被当作正常最高转速。
- 设置过高可能导致一直无法建立转速基准。

### `--dynamic-speed-stall-rpm`

- 默认值：`20.0`
- 单位：rpm。
- 电批实测转速低于或等于该值时，认为电批接近停转，机械臂请求进给比例变为零。
- 这是防止电批已经堵住、机械臂仍继续向前推的重要保护参数。

### `--dynamic-speed-min-ratio`

- 默认值：`0.10`
- 范围：`0` 到 `1`。
- 在尚未进入纯旋转阶段且电批未判定堵转时，机械臂允许保持的最小速度比例。
- 例如基础速度为 `15 mm/s`，该值为 `0.05` 时，最低动态进给速度约为：

```text
15 × 0.05 = 0.75 mm/s
```

- 数值过大时，后段仍可能推动工件。
- 数值为零时，机械臂可以随电批速度比例逐渐降到完全静止。
- 进入末段纯旋转后，该最小比例不再生效，机械臂速度会直接变为零。

### `--dynamic-speed-filter-alpha`

- 默认值：`0.20`
- 范围：大于 `0` 且不超过 `1`。
- 对机械臂动态速度比例进行指数平滑。
- 数值越大，机械臂跟随电批速度变化越快，但速度变化可能更明显。
- 数值越小，速度变化越平滑，但跟随会更慢。

动态比例主要同时考虑：

```text
实测转速比例 = 实测转速 / 启动后真实最高转速
目标转速比例 = 当前电批目标转速 / 启动后真实最高转速
机械臂请求比例 = min(实测转速比例, 目标转速比例)
```

## 8. 末段只旋转、不平移参数

### `--rotation-only-speed-rpm`

- 默认值：`80.0`
- 单位：rpm。
- 当动态减速已经启动，并且电批目标转速降到该值或以下时，进入末段纯旋转阶段。
- 进入后，程序记录当前 UR TCP 位姿，持续发送该固定位置的 `servoL` 指令。
- 此时机械臂动态速度比例被直接置零，电批继续旋转。
- 该状态一旦进入，本次拧紧过程中不会恢复平移。

调节规律：

- 数值调大：更早停止机械臂平移，更不容易推动工件，但可能在螺丝尚未充分轴向进入时过早停止进给。
- 数值调小：更晚停止机械臂平移，螺丝获得更长时间的轴向跟随，但更容易推动工件。

当前建议从 `80 rpm` 开始。如果仍然推动工件，可尝试 `100 rpm`；如果停止平移太早，可降到 `60 rpm`。

注意：末段纯旋转不是调用 `stopL`。调用 `stopL` 会终止当前 `servoL` 会话，并可能使安全清理逻辑提前向电批发送 `hold`。当前实现通过持续锁定 TCP 位置保持控制会话，不会因为进入纯旋转阶段主动中断电批。

## 9. 最终协调停止参数

### `--screw-hold-delay-s`

- 默认值：`-1.0`
- 单位：秒。
- 控制达到最终力矩后，机械臂停止与电批 `hold` 之间的协调方式。

取值含义：

- 小于 `0`：不使用固定延时；程序发送机械臂停止命令后，读取 UR TCP 实际速度，等待机械臂速度降到同步阈值，再向电批发送 `hold`。
- 等于或大于 `0`：使用固定延时，延时值会被限制在安全范围内。

推荐使用：

```bash
--screw-hold-delay-s -1
```

该参数只影响最终达到力矩后的协调停止，不影响末段纯旋转阶段。

### `--stop-sync-speed-threshold-mm-s`

- 默认值：`0.5`
- 单位：毫米/秒。
- 当 `--screw-hold-delay-s` 为负数时，用于判断机械臂是否已经基本停止。
- UR TCP 实际线速度下降到该值以下后，程序才向电批发送 `hold`。
- 数值越小，同步判定越严格，但电批可能保持旋转更久；数值越大，电批会更早停止。

## 10. 参数之间的重要约束

### 动态减速比例约束

必须满足：

```text
0 < torque-slowdown-ratio < dynamic-screw-full-crawl-ratio < 1
```

例如下面是合法组合：

```text
torque-slowdown-ratio = 0.35
dynamic-screw-full-crawl-ratio = 0.65
```

### 堵转阈值与有效转速阈值

通常建议：

```text
dynamic-speed-stall-rpm < dynamic-speed-min-valid-rpm
```

例如：

```text
dynamic-speed-stall-rpm = 25
dynamic-speed-min-valid-rpm = 30
```

### 纯旋转转速与爬行转速

通常建议：

```text
torque-crawl-rpm ≤ rotation-only-speed-rpm < 实际高速转速
```

例如：

```text
torque-crawl-rpm = 30
rotation-only-speed-rpm = 80
实际高速转速约为 220
```

## 11. 推荐调参顺序

不要同时大幅修改所有参数。建议按以下顺序调整：

1. 用较低力矩和较小移动距离确认 `--insert-sign` 正确。
2. 确认电批在空载下的实际最高转速和 CAN 反馈稳定性。
3. 设置合理的 `--torque-stop-nm`，不要一开始就使用硬件极限。
4. 调整 `--torque-crawl-rpm`，确认低速时能够稳定继续拧紧。
5. 调整 `--torque-slowdown-ratio` 和 `--dynamic-screw-full-crawl-ratio`，使减速区间足够提前。
6. 调整 `--insert-speed-mm-s`，使机械臂前期推进速度与螺丝旋入速度匹配。
7. 调整 `--rotation-only-speed-rpm`，消除后段推动工件的问题。
8. 最后根据停止过冲调整 `--stop-linear-acc-mm-s2` 和同步停止参数。

## 12. 当前推荐完整测试命令

```bash
cd /home/sjh/Desktop/SCREW20260723

/home/sjh/anaconda3/envs/lerobot/bin/python \
  lerobot_robot_screw/tests/test_torque_insert_hardware.py \
  --robot-host 192.168.1.5 \
  --robot-connect-timeout-s 10 \
  --screw-host 127.0.0.1 \
  --screw-port 5055 \
  --screw-timeout-s 2 \
  --max-insert-depth-mm 90 \
  --insert-speed-mm-s 15.0 \
  --linear-acc-mm-s2 20 \
  --stop-linear-acc-mm-s2 2000 \
  --insert-sign 1 \
  --motion-timeout-s 30 \
  --screw-speed-rpm 220 \
  --torque-stop-nm 3.5 \
  --torque-poll-s 0.005 \
  --torque-filter-window 5 \
  --torque-confirm-samples 3 \
  --torque-min-active-s 0.2 \
  --torque-slowdown-ratio 0.35 \
  --torque-crawl-rpm 30 \
  --dynamic-screw-full-crawl-ratio 0.65 \
  --dynamic-screw-speed-prediction-s 0.10 \
  --dynamic-screw-prediction-min-rise-nm 0.15 \
  --dynamic-screw-speed-command-interval-s 0.05 \
  --dynamic-screw-speed-slew-rpm-s 3000 \
  --dynamic-screw-speed-min-command-delta-rpm 20 \
  --dynamic-speed-servo-dt 0.008 \
  --dynamic-speed-min-valid-rpm 30 \
  --dynamic-speed-stall-rpm 25 \
  --dynamic-speed-min-ratio 0.05 \
  --dynamic-speed-filter-alpha 0.20 \
  --rotation-only-speed-rpm 80 \
  --backend-torque-guard-ratio 2.0 \
  --screw-hold-delay-s -1 \
  --stop-sync-speed-threshold-mm-s 0.5
```

## 13. 常见现象与对应参数

### 电批高速阶段没有明显减速

可能原因：设置的目标速度超过硬件实际最高速度。例如目标为 `600 rpm`，实际一直饱和在约 `220 rpm`。

处理建议：

- 查看日志中的 `measured_speed_rpm` 和 `effective_high`。
- 使用接近真实最高转速的 `--screw-speed-rpm`，例如 `220`。
- 适当降低 `--torque-slowdown-ratio`，让减速更早开始。

### 后段机械臂仍推动工件

处理建议：

- 增大 `--rotation-only-speed-rpm`，例如从 `80` 调到 `100`。
- 降低 `--dynamic-speed-min-ratio`。
- 降低 `--insert-speed-mm-s`。

### 机械臂过早停止平移

处理建议：

- 降低 `--rotation-only-speed-rpm`，例如从 `80` 调到 `60`。
- 检查动态电批减速是否过早，可适当增大 `--torque-slowdown-ratio`。
- 检查力矩预测是否过于激进，可减小 `--dynamic-screw-speed-prediction-s`。

### 达不到目标力矩就停止

先查看日志中的停止原因：

- `Screw heartbeat failed` 或 CAN 反馈超时：检查电批服务、CAN-FD、CAN ID 和红灯状态。
- `Insertion moveL stopped before torque threshold`：检查控制会话是否提前结束或 `--motion-timeout-s` 是否过短。
- `Maximum insertion travel reached before torque threshold`：增大允许距离前必须先确认不会撞机；也应检查螺丝是否真正对孔。
- 电批模式变成 `HOLDING` 且无硬件故障：可能是上层安全清理主动发送了 `hold`，不一定是电批硬件自行中断。

### 最终力矩过冲明显

处理建议：

- 降低 `--torque-crawl-rpm`。
- 降低 `--torque-slowdown-ratio`，让减速更早。
- 降低 `--dynamic-screw-full-crawl-ratio`，让电批更早进入爬行速度，但该值必须仍大于 `--torque-slowdown-ratio`。
- 增大预测时间会更早减速，但需要防止噪声误触发。
- 保持较短的 `--torque-poll-s`。

## 14. 日志中关键字段

测试结束后，日志和 CSV 会记录完整过程。重点关注：

- `target_rpm`：电批当前目标转速。
- `measured_rpm`：电批实际转速。
- `torque_nm`：原始有符号力矩。
- `abs_torque_nm`：绝对力矩。
- `filtered_torque`：控制器使用的中值过滤力矩。
- `effective_high`：根据实际反馈得到的有效高速上限。
- `UR_speed`：机械臂当前动态插入速度。
- `feedback_age_ms`：电批 CAN 反馈距当前的时间。
- `fault` 和 `fault_message`：电批是否报告故障。
- `mode`：电批当前模式，如 `FORWARD`、`HOLDING` 或 `FAULT`。

末段纯旋转正常触发时应出现：

```text
Rotation-only tightening: screw command=... rpm is in final crawl stage; UR axial feed stopped
Adaptive insert: UR position locked; screwdriver continues rotating
Adaptive insert: ... ratio=0.000, UR_speed=0.000 mm/s
```
