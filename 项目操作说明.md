# SCREW07091444 项目说明

## 当前定位

本项目是一个分层验证中的自动螺丝装配研究项目，目标硬件为 **UR3 六轴机械臂、Damiao DM4310 无刷电机电批和相机**，数据采集与后续策略学习使用 LeRobot。

当前可运行能力分为两条流程：

```text
基础运动 Demo：人工提供孔前位姿 -> moveJ 接近 -> 键盘工具坐标 XY 对准
              -> 沿工具 Z 轴 moveL 低速预插 -> 记录 TCP 力数据

LeRobot 采集流程：人工/相机提供目标 -> 记录机器人状态和图像/电批状态
                 -> 确认后调用 DM4310 服务 -> 保存 LeRobotDataset
```

## 目前的整体运动架构：
    相机识别孔位
    ↓
    计算孔前接近位姿
    ↓
    MoveIt 计算无碰撞关节路径
    ↓
    RTDE servoJ 驱动实体 UR3 沿规划路径运动
    ↓
    原来的 moveL 完成精确对孔
    ↓
    原来的 servoL + 电批力矩控制完成插入和拧紧

    关节角控制TCP标定点位（tool0延申24cm）到底目标坐标，实际Moveit在规划时考虑的电批和螺丝钉总长度是21.6cm

## 一次性准备（全都在base环境下进行）：
```bash
cd /home/sjh/SCREW_with_Moveit2/ros1_ws
colcon build --symlink-install \
  --packages-select ur3_workcell_scene screw_moveit_integration
```

#### 实体机标定（保证有/home/sjh/SCREW_with_Moveit2/config/ur3_calibration.yaml 文件）
    MoveIt中的机械臂几何参数应当与实体UR3一致。标定文件生成一次即可：
```bash
mkdir -p ~/桌面/ROS2/moveit/config

ros2 launch ur_calibration calibration_correction.launch.py \
  robot_ip:=192.168.1.5 \
  target_filename:=/home/sjh/SCREW_with_Moveit2/config
```
    以后启动MoveIt都使用：
    /home/sjh/SCREW_with_Moveit2/config/ur3_calibration.yaml


 
## 目录职责

```text
apps/screw_demo/       UR3 RTDE、人工对准、预插、力记录、电批客户端
apps/camera/           相机采集、ring 检测、点云拟合、常驻 HTTP 服务
apps/screw_driver/     DM4310/OpenArm CAN C++ 服务端和监控工具
lerobot_robot_screw/   LeRobot 包、数据特征、采集入口和数据工具
scripts/benchmarks/    启动性能实验
artifacts/             数据集、日志、相机结果、模型等运行产物
archive/legacy/        旧版和重复入口，仅用于历史追溯
third_party/           外部依赖，不在本项目重构范围内
```

源码文件开头包含统一项目文件头，运行产物统一写入 `artifacts/`，不应再写回源码目录。

## 环境

```bash
conda env create -f environment.yml
conda activate lerobot
python -m pip install -e ./lerobot_robot_screw
```

检查源码语法：

```bash
python -m compileall -q apps lerobot_robot_screw scripts
```

真实设备还需要 `ur-rtde`、相机 SDK、OpenCV/Open3D/Ultralytics，以及 DM4310 C++ 服务所需的 CAN 驱动和库。具体依赖以当前 Ubuntu 环境和 `apps/screw_driver` 的 CMake 配置为准。

## 推荐入口

先运行：

```bash
python /home/sjh/Desktop/SCREW20260723/apps/screw_demo/main.py --help
```

基础运动流程：

```bash
python /home/sjh/Desktop/SCREW20260723/apps/screw_demo/main.py \
  --enable-robot \
  --robot-host 192.168.1.5 \
  --approach-pose X Y Z RX RY RZ \
  --insert-depth 0.002 \
  --insert-speed 0.003 \
  --insert-sign 1 \
  --force-log-dir /home/sjh/Desktop/SCREW20260723/artifacts/logs/screw_demo
```

这里的 XYZ 是米，旋转向量是弧度；按键交互和状态机说明见根目录 `README.md`。

LeRobot 数据采集流程：

```bash
python /home/sjh/Desktop/SCREW20260723/lerobot_robot_screw/scripts/recording/record_main_insert_dataset.py \
  --enable-robot --robot-host 192.168.1.5 \
  --approach-pose X_MM Y_MM Z_MM RX RY RZ \
  --max-insert-depth 2 --insert-speed 1 --insert-sign 1 \
  --screw-host 127.0.0.1 --screw-port 5055 --screw-speed-rpm 60 \
  --torque-stop-nm TORQUE_THRESHOLD_NM \
  --torque-filter-window 5 --torque-confirm-samples 3 --torque-min-active-s 0.2 \
  --dataset-root /home/sjh/Desktop/SCREW20260723/artifacts/datasets/real_insert_dataset \
  --repo-id local/real_screw_insert_001 --fps 20
```

该记录器的 XYZ、插入深度和插入速度使用毫米、毫米每秒作为命令行单位，进入 UR3 驱动前会转换为米。它会在确认后启动电批，使用前必须先完成独立低速测试。

## 相机入口

一次相机检测：

```bash
python /home/sjh/Desktop/SCREW20260723/apps/camera/ring.py 192.168.1.66
```

启动常驻服务：

```bash
python /home/sjh/SCREW_with_Moveit2/apps/camera/camera_service.py --host 127.0.0.1 --port 5060
```

相机并行数据采集：

```bash
/home/sjh/anaconda3/envs/lerobot_eye/bin/python \
  lerobot_robot_screw/scripts/recording/record_camera_insert_dataset_parallel.py \
  --start-moveit-stack \
  --moveit-kinematics-params-file \
    /home/sjh/SCREW_with_Moveit2/config/ur3_calibration.yaml \
  --camera-ip 192.168.1.66 \
  --target-ring inner \
  --camera-service-url http://127.0.0.1:5060/capture \
  --skip-screwdriver \
  -- \
  --enable-robot \
  --robot-host 192.168.1.5 \
  --max-insert-depth 90 \
  --insert-speed 15.0 \
  --insert-sign 1 \
  --screw-host 127.0.0.1 \
  --screw-port 5055 \
  --screw-speed-rpm 220 \
  --torque-stop-nm 5.0 \
  --torque-poll-s 0.005 \
  --torque-filter-window 5 \
  --torque-confirm-samples 3 \
  --torque-min-active-s 0.2 \
  --torque-slowdown-ratio 0.35 \
  --torque-crawl-rpm 30 \
  --dynamic-screw-full-crawl-ratio 0.65 \
  --dynamic-speed-stall-rpm 25 \
  --dynamic-speed-min-ratio 0.05 \
  --rotation-only-speed-rpm 150 \
  --stop-linear-acc 2000 \
  --joint-speed 0.5 \
  --joint-acc 0.5 \
  --fps 20
--start-moveit-stack:自动启动MoveGroup、RViz、机械臂模型、电批碰撞体和障碍物

--moveit-kinematics-params-file:让MoveIt使用当前实体UR3的运动学标定，而不是通用UR3理论尺寸

--screw-speed-rpm
    电批转速 
    
--joint-speed
    关节移动速度

--joint-acc
    关节移动加速度

--max-insert-depth
    最大安全插入行程，单位 mm。
    这是安全上限，不是正常拧紧完成条件。

--torque-stop-nm
    电批扭矩停止阈值，单位 Nm。
    真实机器人模式下必须大于 0。

--torque-poll-s
    电批扭矩轮询周期，默认 0.02 秒。

--torque-filter-window / --torque-confirm-samples / --torque-min-active-s
    对绝对扭矩使用中值滤波，并要求连续多次超阈值；同时忽略启动瞬态。

--stop-linear-acc
    达到扭矩阈值时 UR stopL 的减速度，单位 mm/s²。
    默认 500 mm/s²。
```

没有有效手眼标定文件时，程序中的默认接近位姿只能用于流程或性能测试，不能用于真实自动对孔。

## 电批 C++ 服务

`screw_driver` 用于运行电批 C++ 控制服务，并通过 Socket 向 Python 提供命令接口。

重新编译后必须停止旧服务并启动新二进制，真实机器人流程会检查
`feedback_count` 和 `feedback_age_ms`，拒绝连接不含反馈新鲜度保护的旧服务：

```bash
cmake -S apps/screw_driver -B apps/screw_driver/build-openarm -DUSE_OPENARM_CAN=ON
cmake --build apps/screw_driver/build-openarm -j2

```
连接can总线：

```bash
sudo ip link set can0 down
sudo ip link set can0 type can bitrate 1000000 dbitrate 5000000 fd on
sudo ip link set can0 up
```
起服务:
```bash
apps/screw_driver/build-openarm/screw_server \
  --openarm \
  --can can0 \
  --send-id 0x06 \
  --recv-id 0x16 \
  --motor-type 1 \
  --port 5055 \
  --feedback-timeout 0.1
```

旋转电批：
```bash
cd ~/Desktop/SCREW20260723

PYTHONPATH=apps/screw_demo python3 -c '
import time
from screw_client import make_screw_client

with make_screw_client("127.0.0.1", 5055) as client:
    print("启动前状态:", client.status())
    try:
        print("开始持续旋转:", client.set_speed(-10))
        print("正在旋转；按 Ctrl+C 停止")
        while True:
            client.heartbeat()
            time.sleep(0.1)
    except KeyboardInterrupt:
        print("收到 Ctrl+C")
    finally:
        print("停止:", client.hold())
        print("停止后状态:", client.status())
'
其中client.set_speed(-60)是控制旋转速度的
```
检查can总线连接：

```bash
ip addr show can0
```

### 扭矩控制分层测试

统一测试脚本已经移动到：

```text
lerobot_robot_screw/tests/test_torque_insert_hardware.py
```

```bash
cd /home/sjh/Desktop/SCREW20260723

/home/sjh/anaconda3/envs/lerobot/bin/python \
  lerobot_robot_screw/tests/test_torque_insert_hardware.py \
  --max-insert-depth-mm 90 \
  --insert-speed-mm-s 15.0 \
  --screw-speed-rpm 280 \
  --torque-stop-nm 5.0 \
  --torque-slowdown-ratio 0.35 \
  --torque-crawl-rpm 30 \
  --dynamic-screw-full-crawl-ratio 0.65 \
  --dynamic-speed-stall-rpm 25 \
  --dynamic-speed-min-ratio 0.05 \
  --rotation-only-speed-rpm 150
```

## 启动Rviz模拟：
```bash
启动Rviz
source ros1_ws/devel/setup.bash

roslaunch screw_moveit_integration moveit_rtde_planning.launch \
  start_rviz:=true \
  kinematics_params_file:=/home/sjh/SCREW_with_Moveit2/config/ur3_calibration.yaml

开启另一个终端，发布目标点位
source ros1_ws/devel/setup.bash

/usr/bin/python3 \
  ros1_ws/src/screw_moveit_integration/scripts/publish_tcp_markers.py \
  --frame base \
  --tool-link tool0 \
  --tcp-offset 0 0 0.247 0 0 0 \
  --target -0.116 -0.552 0.056 0.002 2.145 -2.186
```


每次运行的终端日志和逐状态力矩 CSV 都保存在
`artifacts/电批力矩测试log/`，结束时终端会用中文打印停止原因和本次收到的最大绝对力矩。

停止原因会区分：电批力矩达到设定阈值、机械臂到达最大移动距离、机械臂保护性停止、
机械臂急停或安全防护停止、电批 CAN 反馈超时、电批故障或急停、达到最大运行时间、
用户取消或 Ctrl+C、moveL 失败、通信或心跳失败以及未分类异常。




运行模拟服务：

```bash
./build/screw_server --port 5055
```

测试 Python 客户端的 dry-run：

```bash
cd SCREW/screw_demo

python tests/test_screw_client.py --dry-run
```

真实电批模式需要在 Ubuntu 工控机上完成 SocketCAN、`openarm_can` 和 C++
服务端配置。运行真实硬件前，必须确认服务端已实现：

- Python 客户端断开后的速度指令看门狗。
- 急停和故障状态下持续发送零速或禁用电机。
- 正确的 JSON 协议解析。
- CAN 反馈超时和通信故障检测。
- 速度、角度、扭矩和电机状态反馈。
